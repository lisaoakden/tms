<% unless @enrollment.blank? || @enrollment.activated? %>
  <% @intro_toggle_btn_value = "Hide" %>
	<% @intro_display = "block" %>
	<%= button_to "Start Course", activate_path(current_user.id, @enrollment.id,"activate"), 
		method: :patch, class: "btn btn-large btn-primary" %>
<% end %>