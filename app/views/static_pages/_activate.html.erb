<% if @enrollment %>
  <% unless enrollment_activation? @enrollment %>
    <% @intro_toggle_btn_value = "Hide" %>
  	<% @intro_display = "block" %>
  	<%= button_to "Start Course", user_enrollment_path(current_user.id,@enrollment.id), method: :patch, class: "btn btn-large btn-primary" %>
  <% end %>
<% end %>