<% if @enrollment %>
  <% unless @enrollment.activated? %>
  	<%= link_to "Start Course", trainee_enrollment_activate_path(current_trainee.id, @enrollment.id, "activate"), method: :patch, class: "btn btn-large btn-primary" %>
	<% end %>  		
<% end %>