<% if @enrollment %>
  <% unless @enrollment.activated? %>
  	<%= link_to "Start Course", trainee_enrollment_path(current_trainee.id, @enrollment.id), method: :patch, class: "btn btn-large btn-primary" %>
	<% end %>  		
<% end %>